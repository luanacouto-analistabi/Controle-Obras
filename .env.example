# Painel do projeto Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Somente para scripts server-side administrativos (seed, migrations via API).
# NUNCA expor no client — não usar o prefixo NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=

# APIs Maua SCP (Timesheet, EAP) — consumidas ao vivo, servidor-only.
MAUA_SCP_API_BASE_URL=https://scp.estaleiromaua.ind.br/_api
MAUA_SCP_API_TOKEN=
